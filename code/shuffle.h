#ifndef SHUFFLE_H
#define SHUFFLE_H

#include "jogo.h"

/**
 * Function to shuffle `AGENT` objects
 *
 * @param agentsShuffle Array of agents to shuffle.
 * @param NAGENTS Total number of agents.
 * */
void shuffle(AGENT **agentsShuffle, int NAGENTS);

#endif
